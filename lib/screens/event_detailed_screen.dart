import 'package:flutter/material.dart';

class EventDetailedScreen extends StatelessWidget {
  const EventDetailedScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(''),
    );
  }
}
